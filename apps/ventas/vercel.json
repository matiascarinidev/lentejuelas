{
    "buildCommand": "pnpm exec prisma generate --schema=prisma/schema.prisma && pnpm --filter @lentejuelas/ventas run build",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "framework": "nextjs"
}