{
    "buildCommand": "pnpm exec prisma db push --accept-data-loss --schema=prisma/schema.prisma && pnpm --filter @lentejuelas/ventas run build",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "framework": "nextjs"
}