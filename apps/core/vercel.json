{
    "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && cd apps/core && npx prisma generate && next build",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "framework": "nextjs"
}