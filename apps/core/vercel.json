{
    "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && cd apps/core && npx prisma generate && pnpm --filter @lentejuelas/core run build",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "framework": "nextjs"
}